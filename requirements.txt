# Email Tracker Server Dependencies
flask>=2.0.0
flask-cors>=3.0.0
gunicorn>=20.1.0
# psycopg2 is only required for PostgreSQL deployments (e.g. Railway).
# For local SQLite usage, this can be omitted.
psycopg2-binary>=2.9.0
